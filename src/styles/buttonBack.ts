import { StyleSheet } from "react-native";

export const backButtonStyles = StyleSheet.create({
    backButton: {
        flexDirection: "row",
        alignItems: "center",
        margin: 12
    },
    backText: {
        color: "#fff",
        fontSize: 16,
        marginLeft: 8,
    },
});